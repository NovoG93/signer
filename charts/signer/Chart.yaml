apiVersion: v2
name: signer
description: A Kubernetes controller that implements a Signer for PodCertificateRequest (PCR) resources, generating and managing X.509 certificates for pods using a self-signed CA.
type: application
version: 0.0.1
appVersion: "0.1.0"
