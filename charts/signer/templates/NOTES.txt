1. Successfully deployed the Lab Signer Controller
   Release: {{ .Release.Name }}
   Namespace: {{ .Release.Namespace }}

2. Verify the deployment:
   kubectl get deployment {{ include "signer.fullname" . }} -n {{ .Release.Namespace }}

3. Check the controller logs:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "signer.name" . }} -f

4. Verify RBAC permissions:
   kubectl get clusterrole {{ include "signer.fullname" . }}-role
   kubectl get clusterrolebinding {{ include "signer.fullname" . }}-rolebinding

5. The controller will now watch for PodCertificateRequest resources with signerName: "{{ .Values.env.signerName | default "novog93.ghcr/signer" }}"

